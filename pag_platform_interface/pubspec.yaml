name: pag_platform_interface
description: "A common platform interface for the pag_flutter plugin."
version: 0.0.1-dev.0
repository: "https://github.com/yanshouwang/pag"
issue_tracker: "https://github.com/yanshouwang/pag/issues"
topics:
  - libpag
  - pag

environment:
  sdk: ^3.6.0
  flutter: ">=3.3.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
